//A. Raising Bacteria - Codeforce

#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    cin >> x;
    cout << __builtin_popcount(x) << endl;//builtin popcount counts the total number of bits(1s) in a binary number
    return 0;
}
